{
  "name": "tenno-hub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host=0.0.0.0 --port=80",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build",
    "validate:sources": "tsx ./scripts/validateSources.cli.ts",
    "update:data": "node --import tsx/esm scripts/updateExternalRawData.ts all",
    "update:data:check": "node --import tsx/esm scripts/updateExternalRawData.ts all --dry-run",
    "update:data:warframe-items": "node --import tsx/esm scripts/updateExternalRawData.ts warframe-items",
    "update:data:drop-data": "node --import tsx/esm scripts/updateExternalRawData.ts warframe-drop-data",
    "generate:wiki-blueprints": "node --import tsx/esm scripts/generate/generateWikiBlueprints.ts",
    "generate:data": "tsx scripts/generate/generateLeanItems.ts && tsx scripts/generate/generateLeanWarframeAll.ts && tsx scripts/generate/generateLeanMods.ts && tsx scripts/generate/generateWfcdItems.ts && tsx scripts/generate/generateWfcdRequirements.ts && tsx scripts/generate/generateRelicMissionIndex.ts && tsx scripts/generate/generateWfcdAcquisition.ts && tsx scripts/generate/generateSourceLabelMap.ts && tsx scripts/generate/generateUnifiedItemAcquisition.ts",
    "refresh:data": "npm run update:data && npm run generate:data",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "x-script-docs": {
    "_note": "See DATA_WORKFLOW.md for the full raw-data, generated-data, and wiki-blueprint workflow.",
    "dev": "Run the Vite development server for local UI work.",
    "build": "Type-check and produce a production build.",
    "lint": "Run ESLint across the repo.",
    "preview": "Preview the production build locally.",
    "validate:sources": "Run source validation checks for acquisition/source data.",
    "update:data": "Safely refresh both external WFCD raw-data folders from GitHub raw URLs using temp downloads plus validation before overwrite.",
    "update:data:check": "Dry-run the external raw-data updater and report which files would change without writing anything.",
    "update:data:warframe-items": "Safely refresh only external/warframe-items/raw from the configured raw GitHub base URL.",
    "update:data:drop-data": "Safely refresh only external/warframe-drop-data/raw from the configured raw GitHub base URL.",
    "generate:wiki-blueprints": "Convert the Lua-style wiki blueprint dump in external/wiki/blueprintsDatabase.txt into generated JSON for app use.",
    "generate:data": "Regenerate all primary _generated data artifacts from the current external sources, including the unified runtime acquisition index and lean All.json runtime subset.",
    "refresh:data": "Run the raw-data updater and then regenerate all derived _generated artifacts.",
    "test": "Run the Vitest suite once.",
    "test:watch": "Run Vitest in watch mode."
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "date-fns": "^4.1.0",
    "immer": "^11.1.3",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "zod": "^4.3.5",
    "zustand": "^5.0.9"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@tailwindcss/cli": "^4.1.18",
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "autoprefixer": "^10.4.23",
    "cheerio": "^1.1.2",
    "eslint": "^9.39.1",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.24",
    "gh-pages": "^6.3.0",
    "globals": "^16.5.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.46.4",
    "vite": "^7.2.4",
    "vitest": "^4.1.0",
    "warframe-items": "^1.1180.0"
  }
}
